# Server Configuration
PORT=8080
ENV=development

# Firebase (uncomment when configured)
# FIREBASE_PROJECT_ID=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=config/firebase-admin-sdk.json

# API Keys (if needed)
# API_KEY=your-secret-key
